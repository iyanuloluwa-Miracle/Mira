# workflows

CI pipelines: lint, typecheck, unit/integration/e2e tests, CodeQL, and a scan that fails the build on secret or participant-data patterns. Every workflow here must pass before merge to main.
