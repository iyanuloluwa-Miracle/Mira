# docs/screenshots

Screenshots referenced by docs (e.g. evaluation evidence). Optional — never include images containing real participant data (R10).

Produced by `npx tsx scripts/capture-evidence.ts` after `npm run demo` has seeded the database —
see [docs/chapter-four-inputs.md](../chapter-four-inputs.md#screenshot-index) for the expected
filename-to-screen mapping.
