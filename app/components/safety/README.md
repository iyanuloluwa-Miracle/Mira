# components/safety

The crisis exit control and escalation banners (R3). Must render with no loading state and stay reachable from every screen — this is the one directory where static and boring beats clever.
