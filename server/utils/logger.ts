// [R4] Application logger, wrapped in a redactor. No plaintext free text, chat content,
// identifier, or token may ever reach a log line — the redactor enforces that and is
// itself tested (rule R4).

export {}
