# server/domain

Pure business logic: no Nuxt, Nitro, Prisma, or network imports. Every function here is a plain input-to-output transform, which is what makes it unit-testable and auditable in isolation.
