// Shared types describing the input/output contract between the domain layer and the
// classifier and LLM services, so server/domain never needs to import those services
// directly. See services/classifier/README.md for the classifier's own contract doc.

export {}
